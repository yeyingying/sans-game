# 上线部署说明

本游戏是纯静态网页（HTML + JS + 图片 + 音乐），不需要服务器，任何静态托管都能跑。
代码已用 git 提交完成，接下来只差「推送到托管平台」这一步——这一步需要你自己的账号登录。

---

## 方案 A：GitHub Pages（免费，国内可访问，最省事）

1. 打开 https://github.com ，注册/登录，点右上角 **+ → New repository**，
   仓库名随意（例如 `sans-game`），选 **Public**，**不要**勾选 "Add a README"，点 Create。

2. 回到本机终端，在项目目录 `~/undertale` 里执行（把下面的地址换成你新建仓库的地址）：
   ```bash
   git branch -M main
   git remote add origin https://github.com/你的用户名/sans-game.git
   git push -u origin main
   ```
   期间会让你登录 GitHub（浏览器授权或输入用户名 + Personal Access Token）。

3. 在 GitHub 仓库页面点 **Settings → Pages**，
   Source 选 **Deploy from a branch**，分支选 **main**、目录选 **/(root)**，Save。

4. 等 1~2 分钟，页面上会出现网址 `https://你的用户名.github.io/sans-game/`，
   打开就能玩，手机电脑都行，把链接发给别人即可。

---

## 方案 B：Cloudflare Pages（免费，速度通常比 GitHub Pages 稳）

1. 先按方案 A 的第 1~2 步把代码推到 GitHub。
2. 打开 https://pages.cloudflare.com ，用邮箱注册，
   **Create a project → Connect to Git**，授权并选中你的仓库。
3. 构建设置全部留空（Framework 选 None，Build command 留空，Output 留空），Deploy。
4. 拿到 `xxx.pages.dev` 的网址即可。

---

## 想要更快的国内访问（可选，进阶）

GitHub Pages / Cloudflare 在国内能打开但速度一般。若要又快又稳：
- 买一个域名（阿里云/腾讯云，每年几十元）；
- 做 **ICP 备案**（本人实名，约 2~4 周，只能你本人办）；
- 把静态文件上传到阿里云 OSS / 腾讯云 COS 的静态网站托管。
这套流程我可以一步步指导你操作。

---

## ⚠️ 关于版权（重要）

游戏里用到了 Undertale 的角色（Sans）、从原作提取的贴图、以及 MEGALOVANIA 等原曲。
- **免费、非商业地挂在网页上给人玩**：Toby Fox 对同人作品一向宽容，一般没问题。
- **微信小游戏 / 抖音小游戏等平台**：需要企业资质、软件著作权、通常还要游戏版号，
  且同人素材过不了审、有法律风险——**不建议**上这些平台，除非把角色、贴图、音乐
  全部替换成你自己的原创素材。

---

## 本地预览

```bash
cd ~/undertale
python3 serve.py     # 然后浏览器打开 http://localhost:8765/index.html
```
