# 部署到阿里云 OSS 香港区（免备案，国内可访问）

**要上传的文件**：就是本项目里 `deploy/` 文件夹里的全部内容
（`index.html`、`style.css`、`src/` 文件夹、3 个 mp3），总共约 9MB。
其它图片文件都已烘焙进代码，不用传。

---

## 一次性准备

1. 打开 https://www.aliyun.com ，注册账号并完成**实名认证**
   （这是阿里云账号级别的实名，刷个身份证即可，**不是网站备案**，别搞混）。
2. 搜索并开通 **对象存储 OSS**（有免费额度，超出也很便宜，见文末）。

## 创建 Bucket 并上传

3. 进入 OSS 控制台 → **创建 Bucket**：
   - 名称：随便起，例如 `sans-game`（全网唯一，被占用就换一个）
   - **地域(Region)：选「中国香港」** ← 关键，香港区免备案且能正常显示网页
   - 读写权限：选 **公共读**
   - 其它默认，创建。

4. 进入这个 Bucket → **文件管理 / 对象** → **上传文件**：
   - 把 `deploy/` 里的 `index.html`、`style.css`、3 个 `.mp3` 上传到根目录；
   - 再新建目录 `src`，把 `deploy/src/` 里的 9 个 `.js` 全部上传进去。
   - （或者直接用「上传文件夹」把 `deploy` 里的东西整体拖进去，注意保持
     `src/` 的目录结构。）

## 开启静态网站 + 拿到网址

5. Bucket 左侧菜单 → **数据处理 → 静态网站托管**（有的版本在「基础设置」里）：
   - 默认首页填 `index.html`
   - 默认 404 页也填 `index.html`，保存。

6. 拿网址：Bucket → 概览 页会显示 **Bucket 域名 / 外网访问 Endpoint**，
   你的游戏地址就是：
   ```
   https://你的bucket名.oss-cn-hongkong.aliyuncs.com/index.html
   ```
   直接在手机/电脑浏览器打开就能玩，把这个链接发给别人即可，**无需翻墙**。

---

## 让链接更好看（可选）

- 上面那串 `oss-cn-hongkong.aliyuncs.com` 的网址能用但难记。
- 如果想要 `game.你的域名.com` 这种：需要买个域名，然后在 OSS 里「绑定自定义域名」。
  **香港区绑定域名做 CNAME 免备案**（只有走「国内 CDN 加速」才需要备案，我们不用 CDN 就行）。
  买域名 + 绑定的步骤我可以再带你做。

## 费用（很便宜）

- 存储：9MB 几乎为 0（香港区约 ¥0.12/GB/月）。
- 流量：外网流出香港区约 ¥0.5/GB。一次打开游戏约下载 3~5MB，
  就算几百人玩一次也就几块钱。日常和朋友分享基本可以忽略。
- 建议在「费用中心」设个**费用预警**（比如每月 ¥10），超了提醒你，防止意外。

---

## 备选：Gitee Pages（码云，免费、国内、但要审核）

- 国产 GitHub，境内节点、免翻墙很快、**免费**。
- 缺点：需要**实名认证 + 仓库人工审核**才能开 Pages，服务偶尔不稳定。
- 适合完全不想花钱、能接受审核等待的情况。需要的话我给你单独的步骤。
